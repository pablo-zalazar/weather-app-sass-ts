export interface nowInterface {
  name: string;
  desc: string;
  date: string;
  temp: number;
  hum: number;
  icon: string;
}

export interface nextHoursInterface {
  date: string;
  desc: string;
  temp: number;
  icon: string;
}
